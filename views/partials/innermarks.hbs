<div class="ui horizontal segments">
  {{#each bookmarks.innermark}}
  <section class="ui segment">
      <img src="/images/wireframe/image.png">
      <h2>{{title}}</h2>
    <div class="ui grid">
      <div class="fourteen wide column"><a href="{{link}}" target="_blank" class="ui primary button">Visit {{title}}</a></div>
      <div class="two wide column center aligned middle aligned"><a href="/bookmarks/{{../bookmarks.id}}/deleteinnermark/{{id}}"><i class="large red icon trash"></i></a></div>
      
        
    </div>
    </section>
  {{/each}}
</div>